namespace TypingCore.Models;

/// <summary>
/// Identifies the visual theme preferred by the user.
/// </summary>
/// <remarks>
/// Enum values are immutable and thread-safe.
/// </remarks>
public enum UserTheme
{
    FollowSystem = 0,
    Light = 1,
    Dark = 2,
}
